<!DOCTYPE html>
<html lang="pt-br"">
<head>
    <meta charset=" UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Alunos</title>
</head>

<body>
   <h1><%= instrumento ? `Alunos de ${instrumento}`:`Todos os alunos`%></h1> <!--Lógica para mostrar o título dependendo do conteúdo da página-->

    <ul>
        <% for (let aluno of alunos) { %>
            <li><a href="/alunos/<%= aluno._id %>">
                    <%= aluno.nome %>
                </a></li>
        <% } %>
    </ul>
    <a href="/alunos/novo">Novo aluno</a>

    <!--lógica para que o link para todos os alunos só apareça quando estiver filtrado por instrumento-->
    <% if (instrumento) { %>
        <a href="/alunos">Todos os alunos</a>
    <% } %>
</body>

</html>